import * as React from 'react';

import './app.scss';
import Tile from './tile/tile';

const app: React.FC = () => {
    return(
        <div>
            <Tile />
        </div>
    );
};

export default app;
